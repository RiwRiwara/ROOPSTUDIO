# RoopStudio — คู่มือใช้งาน

สั่ง AI ให้แต่งรูปผ่าน Photopea แล้วเห็นทุกขั้นที่มันทำ — เอกสารนี้ครอบคลุมการติดตั้ง
การต่อกับ Claude Code / Claude Desktop ตัวอย่างการใช้จริง และวิธีแก้เมื่อติดปัญหา

---

## สารบัญ

1. [ติดตั้ง](#1-ติดตั้ง)
2. [ต่อกับ Claude Code (CLI)](#2-ต่อกับ-claude-code-cli)
3. [ต่อกับ Claude Desktop](#3-ต่อกับ-claude-desktop)
4. [มันทำงานยังไง](#4-มันทำงานยังไง)
5. [หน้า Studio มีอะไรบ้าง](#5-หน้า-studio-มีอะไรบ้าง)
6. [Use case — สั่งงานจริงพูดว่าอะไร](#6-use-case--สั่งงานจริงพูดว่าอะไร)
7. [รายการ tool ทั้งหมด](#7-รายการ-tool-ทั้งหมด)
8. [เคล็ดลับให้ AI ทำถูกตั้งแต่ครั้งแรก](#8-เคล็ดลับให้-ai-ทำถูกตั้งแต่ครั้งแรก)
9. [แก้ปัญหา](#9-แก้ปัญหา)
10. [โหมดพิเศษ](#10-โหมดพิเศษ)

---

## 1. ติดตั้ง

ต้องมี **Node 18 ขึ้นไป** อย่างเดียว (เช็คด้วย `node -v`)

```bash
cd path/to/roopstudio/app
pnpm install          # หรือ npm install ก็ได้ — มี dependency แค่ 3 ตัว
```

ลองว่ารันได้ไหมก่อนต่อกับ AI:

```bash
node src/index.js --mock --ui-only
```

เบราว์เซอร์จะเปิดหน้า Studio พร้อม Photopea จำลอง (ไม่ต้องต่อเน็ต) กดปุ่มลัดเล่นดูได้
ถ้าเห็นหน้าจอนี้แปลว่าพร้อม — กด `Ctrl+C` ปิดแล้วไปขั้นต่อไป

> **จำพาธเต็มไว้** เดี๋ยวต้องใช้ หาได้ด้วย `pwd` ตอนอยู่ในโฟลเดอร์ `app`
> เช่น `/Volumes/Riw SSD/ExProject/Aioneday/projects/030826_roopstudio/app`
> พาธที่มีช่องว่าง (เช่น `Riw SSD`) **ต้องใส่เครื่องหมายคำพูดครอบเสมอ**

---

## 2. ต่อกับ Claude Code (CLI)

### วิธีที่ง่ายที่สุด

```bash
claude mcp add roopstudio -- node "/path/to/030826_roopstudio/app/src/index.js"
```

`--` คือตัวคั่นสำคัญ: ทุกอย่างหลังจากนั้นคือคำสั่งที่ใช้เปิด server
ถ้าไม่ใส่ Claude จะพยายามอ่าน flag ของเราเป็นของตัวเองแล้วพัง

เช็คว่าติดไหม:

```bash
claude mcp list          # ควรขึ้น  ✔ Connected: roopstudio
claude mcp get roopstudio
```

ในเซสชัน Claude Code พิมพ์ `/mcp` เพื่อดูสถานะ เปิด/ปิด หรือสั่งเชื่อมต่อใหม่ได้

### เลือก scope

| scope | ใช้ได้ที่ไหน | เก็บไว้ที่ | คำสั่ง |
|---|---|---|---|
| local (ค่าเริ่มต้น) | เฉพาะโปรเจกต์ที่รันคำสั่ง | `~/.claude.json` | `--scope local` |
| user | ทุกโปรเจกต์ของเรา | `~/.claude.json` | `--scope user` |
| project | โปรเจกต์นี้ + แชร์กับทีมผ่าน git | `.mcp.json` ในโปรเจกต์ | `--scope project` |

RoopStudio เป็นเครื่องมือที่อยากเรียกได้จากทุกที่ → แนะนำ **user**:

```bash
claude mcp add --scope user roopstudio -- node "/path/to/app/src/index.js"
```

### หรือเขียน config เอง

`.mcp.json` ที่ root ของโปรเจกต์ (scope project):

```json
{
  "mcpServers": {
    "roopstudio": {
      "type": "stdio",
      "command": "node",
      "args": ["/path/to/030826_roopstudio/app/src/index.js"]
    }
  }
}
```

ส่งพารามิเตอร์เพิ่มก็ใส่ต่อใน `args` เช่นเปลี่ยนพอร์ต:

```json
"args": ["/path/to/app/src/index.js", "--port", "4200"]
```

หรือใช้ env แทน:

```json
"env": { "ROOPSTUDIO_PORT": "4200" }
```

เซิร์ฟเวอร์ที่มาจาก `.mcp.json` Claude Code จะถามอนุมัติครั้งแรก — อนุมัติล่วงหน้าได้
ที่ `.claude/settings.json`:

```json
{ "enabledMcpjsonServers": ["roopstudio"] }
```

### ถอนออก

```bash
claude mcp remove roopstudio
```

---

## 3. ต่อกับ Claude Desktop

เปิดไฟล์ config:

- **macOS** — `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows** — `%APPDATA%\Claude\claude_desktop_config.json`

หาไม่เจอก็เปิดจากในแอปได้: **Settings → Developer → Edit Config**

ใส่แบบนี้ (ถ้ามี `mcpServers` อยู่แล้วให้เพิ่มเข้าไปในนั้น อย่าเขียนทับทั้งก้อน):

```json
{
  "mcpServers": {
    "roopstudio": {
      "command": "node",
      "args": ["/Users/riwara/path/to/030826_roopstudio/app/src/index.js"]
    }
  }
}
```

Windows ต้องใส่ backslash สองตัว:

```json
"args": ["C:\\Users\\me\\roopstudio\\app\\src\\index.js"]
```

จากนั้น **ปิดแอปแล้วเปิดใหม่** (ปิดหน้าต่างเฉย ๆ ไม่พอ — ต้องออกจากโปรแกรมจริง ๆ)
เสร็จแล้วจะเห็นไอคอนเครื่องมือในช่องแชท กดดูได้ว่ามี tool ของ roopstudio โผล่มาไหม

> ถ้าใช้ nvm หรือ Node ที่ไม่ได้อยู่ใน PATH มาตรฐาน Claude Desktop อาจหา `node` ไม่เจอ
> ให้ใส่พาธเต็มของ node แทน เช่น `"command": "/Users/riwara/.nvm/versions/node/v20.11.0/bin/node"`
> (หาได้จาก `which node`)

---

## 4. มันทำงานยังไง

```
คุณพิมพ์สั่ง  →  Claude  →  RoopStudio (MCP)  →  WebSocket  →  หน้า Studio ในเบราว์เซอร์
                                                                        ↓ postMessage
                                                                     Photopea
                                                                        ↓ ไฟล์ที่ export
                                              เซฟลงเครื่อง ← RoopStudio ←
```

สิ่งที่ควรรู้:

- **เบราว์เซอร์เปิดเองตอนมีคำสั่งแรก** ไม่ต้องเปิดค้างไว้ก่อน (ครั้งแรก Photopea โหลดสักครู่
  AI จะรอให้พร้อมเองไม่ต้องกดอะไร)
- **รูปไม่ผ่านเซิร์ฟเวอร์ใคร** — ประมวลผลใน Photopea บนเบราว์เซอร์ของคุณ
  ตัว RoopStudio ฟังเฉพาะ `127.0.0.1` เครื่องอื่นในวงเน็ตมองไม่เห็น
- **ปิดแท็บ = งานหาย** เหมือนปิด Photoshop โดยไม่เซฟ ถ้ายังทำงานอยู่อย่าเพิ่งปิด
- ให้เปิดแท็บ Studio **แท็บเดียว** ถ้าเปิดสองแท็บ แท็บใหม่จะแย่งการเชื่อมต่อไป

---

## 5. หน้า Studio มีอะไรบ้าง

**แถบบน** — สถานะการเชื่อมต่อ (เชื่อมต่อ / Photopea) ติดไฟทั้งคู่แปลว่าพร้อมรับคำสั่ง

**ไทม์ไลน์** — ทุกขั้นที่เกิดขึ้น เรียงจากบนลงล่าง แต่ละแถวบอกว่า tool อะไร ใครสั่ง
(`AI` หรือ `คุณกดเอง`) ใช้เวลากี่ ms และถ้าพังจะขึ้นข้อความ error จริงจาก Photopea
ไม่ใช่แค่ "ล้มเหลว" — ใช้ดูว่า AI เข้าใจที่เราสั่งถูกไหม และพลาดตรงไหน

**แกลเลอรี** — ไฟล์ทุกไฟล์ที่ export ในเซสชันนี้ กดรูปเพื่อดูใหญ่ กด "เซฟ" เพื่อโหลดลงเครื่อง
หรือ "ดาวน์โหลดทั้งหมด" ทีเดียว (ถ้าสั่ง AI ให้เซฟลงโฟลเดอร์อยู่แล้ว อันนี้ไว้ดูตัวอย่างเฉย ๆ)

**ปุ่มลัด** — ทำเองได้โดยไม่ต้องพิมพ์สั่ง AI: export PNG/JPG, ย่อเป็นขนาดโซเชียลแต่ละแบบ,
โหลดฟอนต์ไทย, ย้อน 1 ขั้น, ดูข้อมูลไฟล์ — กดแทรกระหว่าง AI ทำงานได้ ไม่ชนกันเพราะมีคิว

**สำคัญ**: ระหว่าง AI ทำงาน คุณ**แก้รูปเองใน Photopea ได้เลย** มันคือ Photopea ตัวเต็ม
ลากเลเยอร์ ใช้แปรง ปรับสี ได้ตามปกติ แล้วค่อยสั่ง AI ทำต่อจากตรงนั้น

---

## 6. Use case — สั่งงานจริงพูดว่าอะไร

### 6.1 ทำภาพปกโพสต์ครบทุกขนาด (ท่าหลัก)

> ทำภาพปกโพสต์ให้หน่อย พาดหัวว่า "โค้ดเสร็จแล้วแต่ไปต่อไม่เป็น"
> ข้อความรอง "เครื่องมือที่ดูโปรเจกต์จริงแล้วบอกว่าคุณค้างขั้นไหน"
> มุมล่างใส่ AI วันละเจค เอาทุกขนาด เซฟลง ~/Desktop/covers

ได้ครบ OG (1200×630) + สี่เหลี่ยม (1080×1080) + สตอรี่ (1080×1920) โดย**จัดหน้าใหม่
ต่อขนาด** ไม่ใช่ครอปจากภาพเดียว ตัวหนังสือจึงไม่โดนตัดหัวตัดท้าย ฟอนต์ไทยโหลดให้อัตโนมัติ

**มีรูปพื้นหลังด้วย:**

> ทำปกโพสต์จากรูป ~/Pictures/workspace.jpg พาดหัว "ทำไมพิมพ์ไทยแล้วได้กล่องสี่เหลี่ยม"
> เอาแบบทับบนรูปมีแผ่นบัง เฉพาะขนาด og กับ story

**เปลี่ยนธีม/ฟอนต์:**

> เอาแบบพื้นดำตัวขาว ใช้ฟอนต์ Kanit

ธีมมีสามแบบ: `ink` (ตัวดำพื้นครีม), `inverse` (ตัวขาวพื้นดำ), `photo` (ทับรูป+แผ่นบัง)
ฟอนต์: `plex` (IBM Plex Sans Thai — ค่าเริ่มต้น), `sarabun`, `kanit`, `prompt`

### 6.2 รูปเดียว → ทุกขนาดโซเชียล

> เปิดรูป ~/Desktop/product.jpg แล้วทำเป็นขนาด og, square, story
> เซฟลงโฟลเดอร์เดิม ตั้งชื่อ product

เหมาะกับรูปถ่าย/ภาพที่**ไม่มีตัวหนังสือ** เพราะมันย่อให้เต็มกรอบแล้วครอปกลาง
ถ้ามีตัวหนังสือให้ใช้ท่า 6.1 แทน

### 6.3 ใส่ลายน้ำ

> เปิดรูป ~/Pictures/shot.png ใส่ข้อความ AIONEDAY มุมขวาล่าง สีขาว โปร่ง ๆ
> ขนาดเล็ก ๆ แล้ว export เป็น jpg เซฟทับที่เดิม

### 6.4 ปรับรูปเร็ว ๆ

> เปิด ~/Desktop/dark.jpg เพิ่มความสว่าง 25 คอนทราสต์ 10 แล้วเซฟเป็น
> ~/Desktop/dark-fixed.jpg

> ทำรูปนี้เป็นขาวดำ แล้ว export png

### 6.5 ประกอบภาพเอง (คุมละเอียด)

> สร้างเอกสาร 1080x1080 พื้นสี #111111
> วางรูป ~/Pictures/bg.jpg แบบเต็มกรอบ
> ใส่แถบดำโปร่ง 60% ทับทั้งภาพ
> โหลดฟอนต์ไทยแล้วใส่ข้อความ "ไม่กั๊ก และฟรี" ตรงกลาง สีขาว ขนาด 90
> export png เซฟลง ~/Desktop/out.png

AI จะเรียก tool ทีละตัวตามนี้ — ดูมันทำได้สด ๆ ในไทม์ไลน์

### 6.6 ทำงานกับโฟลเดอร์คอนเทนต์ของตัวเอง

> อ่าน projects/030826_roopstudio/project.md เอาชื่อโปรเจกต์กับสรุปมาทำภาพปก
> แล้วเซฟลง projects/030826_roopstudio/assets/

ท่านี้แรง เพราะ Claude อ่านไฟล์ในเครื่องได้อยู่แล้ว → เอาเนื้อหาที่มีมาทำภาพต่อได้เลย
ไม่ต้องพิมพ์พาดหัวซ้ำ

### 6.7 ท่ายาก — บอกให้เขียนสคริปต์เอง

> ใช้ run_script ทำให้เลเยอร์บนสุดหมุน 15 องศา

`roop_run_script` คือทางออกสุดท้ายเมื่อ tool สำเร็จรูปทำไม่ได้ — AI เขียนสคริปต์
แบบ Photoshop ส่งเข้า Photopea ตรง ๆ (ดูสคริปต์ที่มันส่งได้ในไทม์ไลน์)

---

## 7. รายการ tool ทั้งหมด

**งานสำเร็จรูป**

| tool | ทำอะไร |
|---|---|
| `roop_make_post_cover` | ภาพปกโพสต์พร้อมพาดหัว ครบทุกขนาด จัดหน้าใหม่ต่อขนาด |
| `roop_export_social_set` | เอกสารที่เปิดอยู่ → หลายขนาด (ครอปกลาง) |

**ชิ้นส่วน**

| tool | ทำอะไร |
|---|---|
| `roop_status` | Photopea พร้อมไหม เอกสารที่เปิดอยู่คืออะไร |
| `roop_create_document` | สร้างเอกสารเปล่า (ระบุขนาด หรือเลือก preset) |
| `roop_document_info` | ขนาด จำนวนเลเยอร์ รายชื่อเลเยอร์ |
| `roop_open_image` | เปิดรูปจากไฟล์ในเครื่องหรือ URL |
| `roop_place_image` | วางรูปลงเอกสารปัจจุบัน (cover / contain / ขนาดเดิม) |
| `roop_add_text` | เพิ่มเลเยอร์ข้อความ (จุดเดียวหรือกล่องตัดบรรทัด) |
| `roop_add_rect` | สี่เหลี่ยมทึบ — แถบสี แผ่นบัง เส้นคั่น |
| `roop_adjust` | สว่าง/คอนทราสต์ ขาวดำ กลับสี |
| `roop_resize` | image / canvas / cover |
| `roop_export` | export ไฟล์เดียว (เซฟลงเครื่องได้) |
| `roop_load_thai_fonts` | โหลดฟอนต์ไทยเข้า Photopea แล้วคืนชื่อ PostScript |
| `roop_list_fonts` | รายชื่อฟอนต์ที่ใช้ได้ |
| `roop_undo` | ย้อนกลับ |
| `roop_run_script` | สคริปต์ Photopea ดิบ |

---

## 8. เคล็ดลับให้ AI ทำถูกตั้งแต่ครั้งแรก

- **บอกพาธไฟล์เต็ม ๆ** — `~/Desktop/x.jpg` ใช้ได้ แต่ "รูปที่ผมเซฟไว้เมื่อกี้" AI ไม่รู้จัก
- **บอกที่เซฟด้วยเสมอ** ถ้าอยากได้ไฟล์ลงเครื่อง ("เซฟลง ~/Desktop/out") ถ้าไม่บอก
  ไฟล์จะไปอยู่แค่ในแกลเลอรีของหน้า Studio
- **อยากได้พาดหัวสองบรรทัด** ให้บอกตรงจุดที่ต้องการขึ้นบรรทัดใหม่ เช่น
  "ขึ้นบรรทัดใหม่หลังคำว่าแล้ว"
- **ภาษาไทยต้องมีฟอนต์** — `make_post_cover` โหลดให้เอง แต่ถ้าสั่ง `add_text` ตรง ๆ
  ให้บอก "โหลดฟอนต์ไทยก่อน" ไม่งั้นอาจได้กล่องสี่เหลี่ยม
- **สั่งทีละงานใหญ่ ๆ ดีกว่าสั่งย่อย** — "ทำภาพปก..." ครั้งเดียวแม่นกว่าไล่สั่ง
  สร้างเอกสาร → ใส่ข้อความ → export ทีละขั้น
- **ผิดแล้วสั่งย้อนได้** — "ย้อน 2 ขั้น" หรือกดปุ่มลัดในหน้า Studio

---

## 9. แก้ปัญหา

**`claude mcp list` ขึ้น Failed to connect**
รันด้วยมือดูก่อน: `node "/path/to/app/src/index.js"` ถ้าพังจะเห็น error จริง
มักเป็นเพราะ (ก) พาธผิดหรือมีช่องว่างแล้วไม่ได้ครอบด้วยเครื่องหมายคำพูด
(ข) ยังไม่ได้ `pnpm install` (ค) Node เก่ากว่า 18

**Claude Desktop ไม่เห็น tool เลย**
ปิดแอปให้สนิทแล้วเปิดใหม่ (ไม่ใช่แค่ปิดหน้าต่าง) · เช็คว่า JSON ไม่มีคอมมาเกิน ·
ถ้าใช้ nvm ให้ใส่พาธเต็มของ `node`

**เบราว์เซอร์ไม่เปิดเอง**
เปิดเองที่ `http://127.0.0.1:4127` (พอร์ตจริงดูได้จาก log ฝั่ง stderr ของ server)

**ไฟสถานะ "Photopea" ไม่ติด**
Photopea ยังโหลดไม่เสร็จ หรือเน็ตช้า/ถูกบล็อก — ลอง refresh หน้า Studio
ถ้าองค์กรบล็อก photopea.com ใช้ `--mock` ทดสอบระบบได้แต่จะไม่ได้ Photopea จริง

**"หมดเวลารอ Photopea"**
งานหนักเกิน (ภาพใหญ่มาก) หรือแท็บถูกปิดกลางคัน — เปิดหน้า Studio ค้างไว้แล้วสั่งใหม่

**พิมพ์ไทยได้กล่องสี่เหลี่ยม**
ฟอนต์ยังไม่โหลด — สั่ง "โหลดฟอนต์ไทย" หรือกดปุ่มลัด แล้วสั่งใส่ข้อความใหม่

**พอร์ตชนกัน**
มี RoopStudio ตัวเก่าค้างอยู่ ปิดด้วย `pkill -f "src/index.js"` หรือเปลี่ยนพอร์ต
ด้วย `--port 4200`

---

## 10. โหมดพิเศษ

```bash
node src/index.js                  # ปกติ: Photopea จริง + MCP
node src/index.js --mock           # Photopea จำลอง (ออฟไลน์) — ไว้ทดสอบ/เดโม
node src/index.js --ui-only        # เปิดหน้า Studio อย่างเดียว ไม่เริ่ม MCP
node src/index.js --port 4200      # เปลี่ยนพอร์ต
```

`--mock` มีประโยชน์จริงสองอย่าง: เดโมให้คนดูโดยไม่ต้องต่อเน็ต และทดสอบว่าสคริปต์ที่
ระบบสร้างขึ้นถูกต้องไหมโดยไม่ต้องเปิด Photopea จริง (ตัว mock รันสคริปต์จริง ๆ
ถ้าเขียนผิดจะพังให้เห็น)

---

## ยังไม่ได้ยืนยันกับ Photopea ตัวจริง

โค้ดทั้งหมดทดสอบผ่าน mock แล้ว แต่สามข้อนี้ต้องลองกับของจริงก่อนเชื่อ 100%:
ลำดับไฟล์เมื่อ export หลายขนาดในสคริปต์เดียว, ชื่อ PostScript ของฟอนต์หลังโหลด,
และการเรนเดอร์สระ/วรรณยุกต์ไทยใน Photopea — ถ้าเจอปัญหาข้อไหน จดไว้ใน `notes.md`
